import React from 'react';
import ReactDOM from 'react-dom';

class NewComponent extends React.Component {
    render() {
        return (
            <p>This is coming from a component called New Component. Not too original.</p>
        )
    }
}

export default NewComponent;